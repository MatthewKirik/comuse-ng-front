import { HasErrorBoxDirective } from './has-error-box.directive';

describe('HasErrorBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new HasErrorBoxDirective();
    expect(directive).toBeTruthy();
  });
});
